name: Cleanup final trigger
on:
  push:
    paths:
      - '.github/workflows/cleanup-final.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  cleanup:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo cleanup